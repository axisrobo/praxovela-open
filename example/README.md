# example/

Integration examples for PRAXOVELA third-party integrators.

This directory is a placeholder for curated, verified examples. Current contents:

- `curl-api.md` — minimal curl patterns against the axond API (health, run center, knowledge/memory search).

Examples here must stay accurate to the authoritative contract in [`docs/api.md`](../docs/api.md). When the private repo changes an endpoint, update both.
